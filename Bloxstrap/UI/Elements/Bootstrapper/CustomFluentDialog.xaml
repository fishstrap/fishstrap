<base:WpfUiWindow
    x:Class="Bloxstrap.UI.Elements.Bootstrapper.CustomFluentDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:base="clr-namespace:Bloxstrap.UI.Elements.Base"
    xmlns:wgif="http://wpfanimatedgif.codeplex.com"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:resources="clr-namespace:Bloxstrap.Resources"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vms="clr-namespace:Bloxstrap.UI.ViewModels.Bootstrapper"
    Width="520"
    Height="280"
    MinHeight="0"
    Background="{ui:ThemeResource ApplicationBackgroundBrush}"
    Closing="UiWindow_Closing"
    ExtendsContentIntoTitleBar="True"
    ResizeMode="NoResize"
    WindowBackdropType="{Binding Path=WindowBackdropType, Mode=OneTime}"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None"
    mc:Ignorable="d">

    <Window.TaskbarItemInfo>
        <TaskbarItemInfo ProgressState="{Binding Path=TaskbarProgressState}" ProgressValue="{Binding Path=TaskbarProgressValue}" />
    </Window.TaskbarItemInfo>

    <Grid>
        <Image Source="pack://application:,,,/Resources/Froststrap Custom Background.png" Stretch="UniformToFill" Panel.ZIndex="0" IsHitTestVisible="False"/>
        <Rectangle Fill="#59000000" Panel.ZIndex="1" IsHitTestVisible="False"/>
        <Grid Panel.ZIndex="2">
            <ui:TitleBar VerticalAlignment="Top" CanMaximize="False" ShowClose="False" ShowMaximize="False" ShowMinimize="False"/>
            <Border VerticalAlignment="Bottom" Height="85" Width="520">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,1" EndPoint="0,0">
                        <GradientStop Color="#CC0A0A0A" Offset="0.8" />
                        <GradientStop Color="#000A0A0A" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Grid>
                    <Grid Margin="25,20,25,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Grid Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Left">
                            <Border Width="60" Height="60" CornerRadius="12" Background="#15FFFFFF" VerticalAlignment="Center" HorizontalAlignment="Left">
                                <Image Source="{Binding Icon, Mode=OneWay}" Width="48" Height="48" HorizontalAlignment="Center" VerticalAlignment="Center" RenderOptions.BitmapScalingMode="HighQuality"/>
                            </Border>
                        </Grid>

                        <TextBlock Grid.Column="1" VerticalAlignment="Center" FontSize="16" FontWeight="SemiBold" FontFamily="Segoe UI" Margin="15,0,0,0" Text="{Binding Message, Mode=OneWay}" TextTrimming="CharacterEllipsis"/>

                        <Button Grid.Column="2" Width="90" Height="32" Padding="0" Margin="20,0,0,0" Content="{x:Static resources:Strings.Common_Cancel}" Command="{Binding CancelInstallCommand}" IsEnabled="{Binding CancelEnabled}" BorderThickness="0">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="8"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </Grid>
                </Grid>
            </Border>
            <ProgressBar Height="6" VerticalAlignment="Bottom" Margin="0,0,0,-3" Width="520" IsIndeterminate="{Binding ProgressIndeterminate, Mode=OneWay}" Maximum="{Binding ProgressMaximum, Mode=OneWay}" Value="{Binding ProgressValue, Mode=OneWay}"/>
        </Grid>
    </Grid>
</base:WpfUiWindow>